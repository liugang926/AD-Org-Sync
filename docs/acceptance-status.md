# Django 重构验收状态

2026-09-24 更新：PR [#66](https://github.com/liugang926/AD-Org-Sync/pull/66)、[#67](https://github.com/liugang926/AD-Org-Sync/pull/67) 和 [#68](https://github.com/liugang926/AD-Org-Sync/pull/68) 已依次合并并由原 CI/CD 部署。当前运行 Web 与 worker 镜像、`last_successful_image_tag` 均为 #68 的合并提交 `423c5875647af41b4661d72c42b61154c0cf4905`；六项 CI 和生产部署成功，容器健康，公开 HTTPS 的 `/healthz`、`/readyz`、`/login` 通过。[最新部署](https://github.com/liugang926/AD-Org-Sync/actions/runs/35951379219)。此项只证明发布状态，不替代下文业务验收。

同日开启 `T0001919` 的单人密码重置试点：重新完整读取钉钉目录后，工号在 487 人中唯一；实时 LDAPS 唯一匹配已启用、未受保护的 AD 账号，且没有本地绑定。生产环境仅把该员工的钉钉 userId 加入白名单，重建 Web 并验证名单只有一人，然后开启 `sspr_enabled`；`schedule_enabled` 继续关闭。公开 `/sspr`、`/sspr/callback/dingtalk` 和 `/sspr/oauth/start` 均返回 200，显示钉钉验证入口，不向未验证访客展示账号，CSRF Cookie 为 Secure。开启时尚未发生真实员工授权或密码重置，AC-14～17、AC-21 仍待本人操作和审计核验。

本文件记录证据边界，不作为自动宣布验收通过的依据。2026-09-23：旧平台已退役；PR [#61](https://github.com/liugang926/AD-Org-Sync/pull/61) 合并后，Django 版本 `a480636d3dc40e4a9e98ef859831320b50ed7140` 已由原 production runner 部署，六项 CI 和部署任务全部通过。独立核验确认运行 Web 镜像及 `last_successful_image_tag` 均为该 SHA，Web/Nginx 健康，`db_check` 通过；对外 HTTPS 的 `/healthz`、`/readyz`、`/login`、`/sspr` 均返回 200，`/readyz` 的数据库、表结构、worker 检查全为 true。runner 已恢复在线空闲。此前部署已核验生产受限文件权限为 0600、宿主 cron 只有一条应用调度任务、管理员经公开 HTTPS 登录并产生成功审计；本次未重复这些检查。生产 `schedule_enabled=false`、`sspr_enabled=false`，真实企业验收仍未完成。[PR #61 CI 与部署](https://github.com/liugang926/AD-Org-Sync/actions/runs/35860755304)、[独立镜像与健康核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35861204945)

## 真实开发测试环境的联调证据

- 测试 AD 域控重启后，生产容器在 `LDAP_VERIFY_CERT=false` 下完成 TLS 1.3 握手，成功绑定并读取测试 OU 及其 427 个账号；自签证书未导入信任库。[连接验证任务](https://github.com/liugang926/AD-Org-Sync/actions/runs/35823677780)
- 同一任务完整读取钉钉 488 名人员、52 个部门。原项目保存的测试连接凭据迁入生产宿主受限 `.env`，未写入仓库；临时传递用的 GitHub Environment secrets 已删除。
- 新增来源完整性校验后，从生产白名单出口执行只读结构探针：52 个部门的父子关系与子部门列表一致，53 页人员列表的分页标记及游标格式有效，去重后为 488 人。探针只读取钉钉 API 与现有配置，不访问 AD 或写入业务数据库；它验证了当前真实数据与新增校验条件相容，但不替代新版本部署后的完整同步验收。诊断分支已重新停用该任务。[真实目录结构探针](https://github.com/liugang926/AD-Org-Sync/actions/runs/35855480214)
- PR #61 的生产镜像在配置的测试 OU 下重新运行真实钉钉来源的全量预览：82.9 秒读取 488 人、52 个部门，计划为 357 项关联、111 项新建、19 项冲突、1 项跳过及 52 个部门 OU 项。任务因冲突停在 `blocked`，业务操作记录和有效绑定数量均未增加；诊断任务已重新停用。这验证了新来源完整性校验与当前真实目录相容，但未执行任何计划中的 AD 写入。[新镜像全量预览](https://github.com/liugang926/AD-Org-Sync/actions/runs/35861765004)
- 为测试 OU 配置同步根范围并运行一次全量**预览**，耗时约 86 秒。计划包含 358 个关联、111 个新建、19 个冲突及 52 个部门 OU 项。该次全量任务因冲突停在 `blocked`，未执行 AD 写入或绑定。[预览任务](https://github.com/liugang926/AD-Org-Sync/actions/runs/35823938289)、[聚合核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35824229431)
- 对 PR #61 镜像生成的最新预览再次按匿名原因汇总：19 个人员冲突中，11 个主部门不明确、7 个账号命名为空/重复/占用/受保护、1 个匹配字段缺失或重复；部门 OU 冲突为 0。诊断没有输出员工标识或执行写入，任务已停用。这些问题须在人员配置或真实目录数据中逐项处理，不能通过跳过冲突批量执行。[最新冲突汇总](https://github.com/liugang926/AD-Org-Sync/actions/runs/35866168208)
- 全量预览读取 488 人、52 个部门约需 86 秒；指定 `T0001919` 的单人预览仍约需 85 秒，因为当前会先完整采集来源目录。只读探针确认钉钉 `user/list` 对抽样员工返回工号等字段，但不返回 `main_department` 和 `org_email`；抽样的其他字段与 `user/get` 一致。直接以列表结果替代逐人详情会改变主部门和邮箱的判定，故没有采用这种不完整的提速或增加镜像缓存。[列表字段探针](https://github.com/liugang926/AD-Org-Sync/actions/runs/35848224280)、[单人预览](https://github.com/liugang926/AD-Org-Sync/actions/runs/35844754662)
- 生产入口的 HTTPS 网关以 `10.106.1.119` 连接应用 Nginx。此次部署重建了 Nginx，运行配置已包含可信网关及 `X-Forwarded-For` 规则；公网正常与伪造转发头的探针均返回 200，访问日志显示真实客户端地址可用且伪造地址未生效。不同客户端的实际限流额度仍待交叉验证。[网关核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35843174551)
- 员工页面使用的 `dd.requestAuthCode({clientId, corpId})` 参数与[钉钉微应用免登 JSAPI](https://open.dingtalk.com/tools/explorer/jsapi?id=11723)一致；[钉钉官方企业应用示例](https://github.com/open-dingtalk/corp_demo_php#demo%E4%B8%AD%E7%9A%84%E5%85%8D%E7%99%BB%E6%8E%88%E6%9D%83%E6%B5%81%E7%A8%8B)将该 JSAPI 返回的 `code` 交给服务端换取 `userid`，[免登码接口文档](https://s.apifox.cn/apidoc/docs-site/467052/api-9093138)给出了当前所用 `/topapi/v2/user/getuserinfo` 的 `code` 请求与 `result.userid` 响应。所引用的 JSAPI 文件于 2026-09-23 返回 HTTP 200，应用首页已通过只读接口核对，结果见下条。接口契约一致不代表已完成员工客户端实测；真实授权码仍是端到端验收前提。
- 使用当前 AgentId 对应应用凭据调用钉钉应用列表接口，只读核对了微应用首页：移动端与 PC 端均在正确的 HTTPS 域名及 `:9443` 端口，路径均为 `/sspr/callback/dingtalk`。该路径与 `/sspr` 映射到同一个员工页面，启用 SSPR 时均显示钉钉验证表单，因而无需先切换首页。当前移动端地址没有 `corpid=$CORPID$` 占位符；[钉钉 JSAPI 文档](https://open.dingtalk.com/tools/explorer/jsapi?id=11723)将它描述为可添加的参数，本应用由服务器配置 CorpId，且不信任查询字符串身份。只读脚本没有输出查询值或凭据，诊断任务已重新停用；仍须从钉钉工作台取得真实授权码完成验收。[首页路径核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35862677216)
- 专用测试员工 `T0001919` 在钉钉工号与 AD `employeeID` 中唯一匹配，目标为已启用且不受保护的同名 AD 账号。首次单人预览只提出绑定，但会移除现有 OU 路径中的企业名称层。为避免意外移动账号，已把对应钉钉部门显式映射到测试 OU 内的现有路径；重预览只剩一项 `bind`，属性、OU、账号均无需改动，也不需新建 OU。[身份与首次预览](https://github.com/liugang926/AD-Org-Sync/actions/runs/35844754662)、[现有 OU 映射与重预览](https://github.com/liugang926/AD-Org-Sync/actions/runs/35845437138)
- 在 PR #61 部署后再次只读核对 `T0001919`：当前钉钉目录中工号唯一，使用独立的 `employee_id` 匹配规则在 LDAPS 中唯一命中已启用且不受保护的账号；该员工仍无本地绑定，生产 `sspr_enabled=false`、`schedule_enabled=false`。日志仅记录 userId 指纹，未输出完整 userId 或凭据；诊断任务已停用。此项为后续单人 SSPR 验收提供前置条件，不替代工作台授权码和本人密码重置。[专用员工预检](https://github.com/liugang926/AD-Org-Sync/actions/runs/35865383710)
- 再用该员工已确认的钉钉 userId 直接读取实时人员详情，工号仍为 `T0001919`；LDAPS 仍唯一命中可用账号，且没有本地绑定。该次只读预检没有输出原始 userId、修改账号或开启 SSPR；它确认试点白名单应填写 userId 而非工号，仍不验证真实授权码。[试点身份复核](https://github.com/liugang926/AD-Org-Sync/actions/runs/35877145632)
- 该单人预览已执行成功，当时新增一条本地有效绑定，指向预览确认的 AD objectGUID；执行前后的 AD 账号状态一致，未改动账号属性、密码或 OU。这提供了 AC-01 的真实唯一工号关联证据，但不等同于创建、移动、禁用或密码重置验收。[单人执行核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35845848332)
- 使用运行中的数据库在线生成一致性备份，并在无网络的隔离容器中恢复副本；副本通过完整性及迁移检查，保留单组织配置与 `T0001919` 绑定，随后在副本中关闭调度/重置、清理验证会话和未完成任务，再次通过数据库检查。实时 LDAPS 只读查询确认该绑定仍指向原 AD objectGUID；线上数据库没有被替换，也没有把本地恢复当成 AD 回滚。[隔离恢复演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35846968662)
- 为保留“未同步且无本地绑定也可重置”的真实验收条件，随后通过业务解绑流程移除了 `T0001919` 的本地绑定，并排除自动重新认领。当前仍可仅凭该员工钉钉身份和实时 LDAPS 唯一匹配到原 AD 账号；AD 账号未改变。此项只验证授权前的匹配条件，尚未完成钉钉授权码换取及密码修改。[解绑与无绑定匹配](https://github.com/liugang926/AD-Org-Sync/actions/runs/35847310031)
- 在相同测试域的专用 OU 下，生产镜像的 AD 适配器对一次性合成账号完成真实创建与启用、属性更新、跨 OU 移动及禁用；每步核对原 objectGUID，结束时删除该账号与两个专用 OU，并验证工号不再匹配。首次使用过长合成工号时，域控以 `employeeID` 字段约束拒绝创建，未留下对象；缩短后通过。PR #61 为该拒绝补充可定位且不暴露原始 LDAP 诊断的错误提示。该演练没有使用钉钉来源或同步任务，不等同于 AC-07、AC-08 的端到端验收，也没有修改线上业务数据库。[真实 AD 适配器写入与清理](https://github.com/liugang926/AD-Org-Sync/actions/runs/35850158957)、[目录约束诊断](https://github.com/liugang926/AD-Org-Sync/actions/runs/35850013387)
- 随后使用同一生产镜像、真实测试 AD、隔离 SQLite 和合成通讯录来源运行完整业务预览与执行：两名合成人员首次建号成功，重复执行未重复建号；一人换部门后 objectGUID 不变；完整全量缺失时只禁用该人，另一人保持启用。同步期间线上数据库没有这些测试人员或绑定；演练结束时删除两个账号、两个专用 OU 和隔离数据库，生产就绪接口仍为 200。这验证了 AC-07、AC-08 和离职禁用的业务执行路径与真实 AD 写入，但来源由测试适配器提供，不等于真实钉钉通讯录的端到端验收。[隔离业务同步演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35851591216)
- 复用同一隔离业务同步路径，在首个合成 AD 账号创建后保持执行锁 15 秒，同时从公网请求员工 `/sspr` 与 `/readyz`：两者均返回 200，单次响应分别约 0.020 秒和 0.023 秒。该轮创建、重复执行、移动及禁用均成功，专用账号、OU、隔离数据库已清理，线上数据库无测试人员或绑定，诊断任务已重新停用。这补充了 AC-19 在真实 AD 写入期间的页面可访问性证据；未执行同时密码重置，也不能据单次探针承诺稳定吞吐量。[写入期间公网探针](https://github.com/liugang926/AD-Org-Sync/actions/runs/35857498242)
- 后续一次性演练沿用生产镜像和真实测试 AD，在专用 OU 中先禁用创建合成账号，保持禁用完成属性初始化，再启用同一 objectGUID；最终删除合成账号与专用 OU，核对线上业务数据库没有该身份的人员或绑定，`/readyz` 为 200。它验证了 PR #65 所用 AD 适配器操作顺序，但没有运行 PR #65 的 `synchronization.apply`，也没有部署该草稿 PR。[分阶段建号与清理](https://github.com/liugang926/AD-Org-Sync/actions/runs/35874202073)
- 使用 PR #66 的候选提交 `80f06b170d122443d24b6a88b015a81051263825` 构建临时容器，在独立 SQLite 和专用测试 AD OU 中运行完整的 `synchronization` 预览与执行。合成来源人员首次建号后保持禁用，显示名与职位在禁用期间写入，本地绑定保持暂停；重复同步跳过该人且没有重复建号。演练删除测试账号并核对先前失败的两次诊断均未留下测试身份，线上业务数据库无合成人员或绑定，`/readyz` 为 200。该证据覆盖候选代码的分阶段建号执行路径，不等于真实钉钉来源写入，也不表示草稿 PR 已部署。[PR #66 候选同步演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35882277949)
- PR #66 候选提交 `60d9dffd6c6b6a4c0e09a31e5c0345f6340ab422` 又在隔离 SQLite 与真实测试 AD 中模拟“账号已禁用创建、属性尚未写入”时的一次失败。执行记录保存 objectGUID、创建时状态指纹与配置指纹；重新预览只在状态和配置未变时提出续做，随后用同一 objectGUID 完成属性与暂停绑定，重复同步没有重建账号。测试对象已删除，线上业务数据库无测试身份，`/readyz` 为 200。该证据覆盖这一明确的中断点，不代表所有网络超时或 AD 写入后数据库故障都可自动续做。[分阶段建号故障恢复演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35885650579)
- PR #66 候选提交 `0cde22f5490c2e17d1bd04bb5fccaaf696727078` 在生产 runner 上以独立 SQLite 完成真实钉钉来源的指定人员演练：实时核对 `T0001919` 的身份和部门范围，人工将其关联到专用测试 OU 内的一次性 AD 账号；两次完整读取钉钉通讯录后，预览只提出该账号的显示名更新，执行保持原 objectGUID 并写入真实测试 AD。演练结束删除一次性账号，复核该员工原 AD 账号指纹不变、线上业务数据库无测试绑定，公开 `/readyz` 为 200。诊断任务已重新停用。此证据覆盖真实来源驱动的绑定、单人预览与属性写入，不覆盖真实来源的新建、移动、禁用或员工本人密码重置；候选 PR 仍为草稿，未部署。[真实钉钉来源隔离写入演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35887742522)
- 在相同候选代码及隔离方式下，下一轮演练先完成真实来源驱动的显示名更新，再将该员工的人工部门映射改到专用测试 OU 下的新子 OU。新预览明确提出 `move` 且在执行前未移动账号；执行后该一次性 AD 账号进入新 OU，objectGUID、用户名与工号不变。正式员工原 AD 账号指纹仍不变；一次性账号和子 OU 均清除，线上业务数据库无测试绑定，`/readyz` 为 200。诊断任务已重新停用。此项证明人工 OU 映射变化后的移动链路，不等于钉钉实际变更员工部门。[真实来源与人工 OU 映射移动演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35889830463)
- PR #66 候选提交 `ea05ecc995dc55a56eb5cfa2b2f0ee76fa0faa5f` 增加“属性初始化后、账号启用前”的恢复证据。在隔离 SQLite 与真实测试 AD 中，合成来源人员禁用建号并完成显示名、职位写入后，诊断仅让第一次启用失败；重新预览核对原 objectGUID、初始化后 AD 状态指纹及配置指纹，随后启用同一账号并建立有效绑定，重复同步不重建。一次性账号已清除，线上业务数据库无测试身份，`/readyz` 为 200，诊断任务重新停用。该演练只覆盖这一明确中断点；AD 状态或配置变化时自动续做会阻断，网络超时结果不明仍需人工核验。[启用失败后的同 GUID 续做演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35892680590)
- PR #66 后续候选代码将 AD `uSNChanged` 纳入预览和建号恢复的对象指纹：外部修改密码等对象变更或无法读取有效修订号时，自动续做改为冲突待人工核验。隔离测试覆盖禁用建号、已启用但未绑定、预览后变更和修订号缺失。生产容器对专用测试账号的真实 LDAPS 只读探针确认修订号可读取且为有效正整数，生产镜像与就绪状态未变化；探针已停用。该探针仅验证可读性，改密与恢复行为见后续演练。[AD 修订号只读核验](https://github.com/liugang926/AD-Org-Sync/actions/runs/35894980118)
- 在同一专用测试 OU 中，随后对一次性 AD 账号执行受控密码重置；同一 LDAP 连接在改密前后读取的 `uSNChanged` 严格递增。测试账号及专用 OU 已删除，线上业务数据库无测试身份，公网 `/readyz` 仍为 200，诊断任务已停用。这证明该测试域控的密码修改会改变 PR #66 所用指纹输入。[一次性账号改密与 AD 修订号演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35896487041)
- PR #66 候选镜像又在隔离 SQLite 与真实测试 AD 中完成完整阻断演练：合成来源人员建号并初始化后，仅模拟启用前中断；随即通过 LDAPS 修改该禁用账号密码。改密使 `uSNChanged` 和恢复指纹变化，而其他已读取账号字段不变；重新预览将同一 objectGUID 标为冲突，不自动启用、绑定或重建。一次性账号已清除，线上业务数据库无测试身份，本机 `/readyz` 为 200，诊断任务已停用。此证据覆盖该明确中断点和外部密码变化，不代表任意网络超时结果都可自动判定。[改密后恢复阻断演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35897295481)
- 使用候选提交 `b89d265c9fad0308ad0aa4faa8c4680f4b7149fa` 复核人工绑定锁内重读时，真实钉钉部门读取返回错误码 `88`，演练在创建一次性 AD 账号前中止；清理步骤确认测试身份不存在、线上绑定未变、`/readyz` 为 200。随后只读探针在同一生产出口成功读取该员工、配置根部门及员工部门，部门接口均返回 `0`，但无法从这些结果判定先前 `88` 的具体子原因。[中止与清理](https://github.com/liugang926/AD-Org-Sync/actions/runs/35899601784)、[只读复核](https://github.com/liugang926/AD-Org-Sync/actions/runs/35900549961)
- 在当前候选提交 `b72faf0032557970215b8d0648d4e65f65ee0b9a` 上重跑真实钉钉来源演练，生产 runner 的执行及清理步骤均成功：实时确认专用员工身份与部门范围后，在隔离 SQLite 中人工绑定到专用测试 OU 下的一次性 AD 账号；指定人员预览只提出该账号的显示名更新，执行后保持原 GUID；人工 OU 映射调整后的新预览只提出移动，执行后仍保持同一 GUID。脚本逐步核验该员工原 AD 账号指纹不变，并在结束时删除一次性账号与子 OU；清理步骤独立确认线上没有该测试绑定、生产同步和重置仍关闭、`/readyz` 为 200。诊断任务已重新停用。它覆盖最新候选的真实来源绑定、属性更新和人工映射移动，不等于真实来源新建、员工实际部门变更、离职禁用或本人密码重置。[最新候选真实来源写入与清理](https://github.com/liugang926/AD-Org-Sync/actions/runs/35902188901)
- 另在隔离 SQLite 中开启 SSPR，仅对测试 OU 下的一次性合成账号使用合成身份来源执行验证与重置：数据库中没有该人的 `Person` 或 `Binding`，实时 LDAPS 唯一匹配后重置成功，新密码可完成 LDAPS 登录，验证会话已消费且重放被拒绝，审计及隔离数据库未发现明文密码。随后删除测试账号、OU 和隔离数据库；生产 SSPR 始终关闭、线上业务数据库无测试会话，生产就绪接口仍为 200。这验证了无绑定重置及密码生效的业务和目录路径，但未验证真实钉钉授权码或 `T0001919` 本人操作。[隔离本人重置演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35852507908)
- PR #66 候选提交 `193cb4164721561df9ee3483a270d893fd2e0808` 又以隔离 SQLite、合成钉钉身份和测试 OU 内一次性 AD 账号验证共享账号锁：真实 AD 显示名写入完成但仍持锁时，员工改密被拒，验证会话未消费；释放锁后用原会话重试，改密成功并可通过 LDAPS 登录；后续再次更新显示名，新密码仍可登录。审计和隔离数据库未包含明文密码；脚本及独立清理步骤确认一次性账号与 OU 已删除、线上无该测试身份的绑定或会话，生产 SSPR 与调度仍关闭，`/readyz` 为 200。该演练使用 AD 适配器属性写入模拟同步持锁，尚未同时运行完整同步任务或真实员工钉钉授权。[真实 AD 写入与改密锁演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35904808570)
- 后续隔离演练改为运行完整的单人同步预览与执行：真实测试 AD 属性写入已完成、执行线程仍持同一账号锁时，合成身份发起的重置被拒且验证会话未消耗；执行结束后原会话重试改密成功，LDAPS 新密码登录成功，下一次完整同步也未覆盖密码。一次性账号清除、生产业务数据库不变，诊断任务重新停用。这验证了同步执行路径与重置路径在同一账号上的串行行为，仍未覆盖真实员工免登授权。[完整同步与重置并发演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35906672537)
- 另在隔离 SQLite、合成来源身份与真实测试 AD 中，将预览及 SSPR 会话指向一次性账号 A 后删除 A，并以相同 DN、用户名和工号创建新 GUID 的 B。旧预览执行失败且未修改 B 或重新绑定；旧 SSPR 会话被消费并拒绝、B 的密码未改变；新预览因旧绑定而阻断，新验证才可重新识别 B。B 和测试 OU 已清除，生产业务数据库不变，诊断任务重新停用。这验证了同名异 GUID 的真实 AD 阻断行为，仍未覆盖真实钉钉授权。[同名异 GUID 替换演练](https://github.com/liugang926/AD-Org-Sync/actions/runs/35907629645)
- 截至上述隔离演练，定时同步和员工密码重置均关闭；真实钉钉身份授权后的本人密码重置，以及真实来源的新建、真实部门变更引发的移动、禁用和剩余场景还未验收。

## 已有自动化证据

| PRD 范围 | 证据位置与覆盖内容 |
| --- | --- |
| FR-01 来源完整性与范围 | `tests/test_directory.py`、`tests/test_sync.py`：分页错误、父子部门不一致、空目录、局部范围；独立刷新不依赖 AD。真实目录结构已完成只读探针核验 |
| FR-02～05 匹配、命名、绑定 | `tests/test_domain.py`、`tests/test_sync.py`、`tests/test_web.py`：稳定绑定、冲突、邮箱需人工确认、签名确认防替换与重放、预览不绑定；PR #66 候选代码在人工绑定提交的账号锁内再次读取目标，工号、DN、启用状态或保护状态变化时要求重新审核 |
| FR-06 部门 OU | `tests/test_sync.py`：空部门创建、人员换部门后保留 GUID；真实测试 AD 与合成来源的业务同步已验证空部门 OU 和跨 OU 移动；真实钉钉来源配合人工 OU 映射变化已验证既有账号移动及 GUID 不变，尚需真实部门变更验收 |
| FR-07 属性与离职 | `tests/test_sync.py`：空值默认保留、显式清除、局部不禁用、全量阈值确认、初始化策略；隔离业务同步已在真实测试 AD 验证完整全量缺失只禁用目标账号 |
| FR-08 任务与恢复 | `tests/test_sync.py`：计划过期、重复入队、逐人保存、进程中断、局部成功不改变全量标记；本地绑定提交失败后依据建号 GUID 恢复，即使工号改变也不重复建号；候选代码在禁用建号后及属性初始化后的两个明确失败点，均只在 AD 状态与配置未变化时续做，真实测试 AD 演练见上文；启用前外部改密使 AD 修订号变化时重预览转冲突，不自动续做；无可靠 GUID 时阻断并保留未解决证据 |
| FR-09～10 员工重置 | `tests/test_sspr.py`、`tests/test_browser.py`：未同步未绑定员工可重置、服务端身份、GUID/配置/过期/重放/CSRF 检查；跨线程占用同一账号锁时改密被拒且会话未消费，锁释放后可用原会话重试；隔离合成身份已在真实测试 AD 验证密码生效、LDAPS 登录、会话消费和无明文，真实钉钉授权仍待验收 |
| 部署退役保护 | `tests/test_deployment.py`：执行真实部署脚本，替代 Docker/HTTP 命令，验证成功、构建失败、首次发布失败不恢复旧版、Nginx 配置重建失败时回退旧配置与已验证 Django 版本 |
| CI/CD 合约 | `tests/test_operations.py`：原有门禁、部署触发、SHA、备份及数据库检查顺序；需以最新 PR 的远端结果为准 |
| AC-20 备份恢复 | `tests/test_operations.py` 覆盖在线备份、隔离恢复与同名异 GUID 阻断；真实环境已完成在线备份的隔离副本检查，并用实时 LDAPS 核验恢复绑定。未替换线上数据库，也未演练线上切换 |

## PRD 验收项逐项判定

“自动化”表示隔离测试覆盖规则，不等于真实钉钉授权或真实来源端到端验收；草稿 PR 的代码不计入当前生产功能。

| 验收项 | 当前最强证据 | 尚缺或边界 |
| --- | --- | --- |
| AC-01 唯一工号关联 | `T0001919` 的真实单人预览与绑定执行 | 已证实该测试账号；绑定随后按业务流程解除 |
| AC-02 人工绑定优先 | 自动化；真实钉钉员工在隔离数据库中人工关联一次性 AD 账号并完成属性写入；候选代码对审核后 AD 关键状态变化及账号锁内保护状态变化执行阻断 | 尚未验证正式员工的人工修正长期保持；并发外部目录写入无法由本地账号锁完全串行化 |
| AC-03 稳定来源 ID | 变更工号后的 GUID 恢复与领域规则自动化 | 真实来源改名或改工号未演练 |
| AC-04 歧义阻断 | 自动化；真实全量预览阻断 19 人 | 真实多 AD 候选和目标占用写入前复核未逐项演练 |
| AC-05 只读预览 | PR #61 全量预览停在 blocked；PR #66 候选真实来源单人预览只提出一次性账号更新，随后经单独执行写入；自动化 | 预览与执行的隔离证据仅针对指定测试账号 |
| AC-06 计划失效 | 配置、来源、绑定与 AD 变化的自动化 | 真实并发变更未演练 |
| AC-07 创建与重跑 | 合成来源、隔离 SQLite、真实测试 AD 的完整业务演练；PR #66 候选代码还验证了禁用建号、暂停绑定及重复同步跳过；真实来源已验证既有账号属性更新 | 真实钉钉来源的新建账号尚未执行 |
| AC-08 换部门移动 | 合成来源演练验证换部门后 GUID 不变；真实来源配合人工 OU 映射变化验证一次性账号移动后 GUID 不变 | 真实钉钉来源的部门变更尚未执行 |
| AC-09 局部范围保护 | 自动化；真实来源指定人员演练只更新一次性账号、线上业务数据库不变 | 真实部门范围写入未演练 |
| AC-10 不完整来源保护 | 分页异常、空目录与部门结构的自动化；真实目录只读结构探针 | 未在真实 API 故障时发起同步 |
| AC-11 大量禁用阈值 | 阈值确认的自动化 | 未在真实目录制造高风险离职计划 |
| AC-12 部分失败与重试 | 按人保存、建号后 DB 失败、未知 GUID 阻断的自动化；跨层测试模拟 AD 建号已落地但响应丢失，确认后续预览冲突且不重复建号；候选代码在真实测试 AD 中验证属性写入前失败及属性完成后启用失败，均以同 GUID 续做并清理；启用前外部改密后重预览阻断并清理 | 响应丢失为隔离模拟，真实网络超时及真实钉钉来源的端到端重试仍未演练 |
| AC-13 同名异 GUID | 领域规则及恢复检查自动化；真实测试 AD 的 A 删除、同名 B 重建后，旧同步预览及 SSPR 会话均不能修改 B | 来源身份为合成身份；未在生产业务数据上执行对象替换 |
| AC-14 目标替换拒绝 | 服务端身份与输入篡改自动化 | 真实钉钉授权后提交尚未执行 |
| AC-15 会话失效 | 过期、重放、配置与 GUID 变更自动化 | 真实钉钉授权会话尚未执行 |
| AC-16 密码生效与无明文 | 合成身份、隔离 SQLite、真实 LDAPS 改密与重新登录 | 专用员工从钉钉工作台的本人改密尚未完成 |
| AC-17 无匹配与受保护阻断 | 缺失、多匹配、保护、禁用的自动化 | 真实目录异常账号未用于员工入口试验 |
| AC-18 手动与定时串行 | 重复入队、执行锁、任务恢复自动化 | 生产定时同步仍关闭，真实并发未演练 |
| AC-19 同步时员工入口 | 真实测试 AD 写入期间公网 `/sspr` 与 `/readyz` 均为 200；完整单人同步任务持锁时合成身份改密被拒但会话仍可用，执行后重试成功且后续同步未覆盖密码 | 尚未并发运行真实员工钉钉授权重置；公网页面探针与并发改密演练分别进行 |
| AC-20 备份恢复 | 线上一致性备份的隔离副本恢复及实时 GUID 核验 | 未进行线上数据库切换；恢复本地数据不回滚 AD |
| AC-21 未同步无绑定重置 | 合成身份与真实 LDAPS 改密；`T0001919` 无绑定且唯一匹配 | 缺真实钉钉授权码和该员工本人重置 |

## 完成前仍需核验

- 按上表补足真实目录和员工操作证据，不以现有绿灯替代全面验收；保护账号、网络故障、AD 写入后数据库失败、并发同步与员工重置仍需重点核查。
- AD 适配器和使用合成来源的隔离业务同步已用专用测试对象完成创建、重复执行、属性、移动、禁用与清理；真实钉钉来源已在隔离数据库中驱动一次性 AD 账号的指定人员属性更新，并在人工 OU 映射变化后驱动移动；隔离合成身份也完成无绑定重置及真实 LDAPS 新密码登录。还需用可控钉钉测试人员完成真实来源的新建、实际部门变更、禁用与本人授权重置验收。此前同步写入演练对 `T0001919` 的既有 AD 账号保持只读，当前线上该员工仍无绑定；本人密码重置试点单独进行，禁止拿普通员工账号试验。
- 已记录实际规模下全量预览与单人预览耗时，也测得隔离合成同步写入期间的单次公网员工页响应；真实来源指定人员演练完成两次完整采集及属性写入。还需测量员工实际重置与同步并发的结果，并评估不改变字段语义的采集提速。
- 隔离副本恢复与真实 AD 对象核验已完成；如未来需要线上数据库切换，仍须按恢复手册取得明确授权并单独演练停机、替换和重启。
- 宿主定时任务已核验，当前 `schedule_enabled=false`；SSPR 仅对 `T0001919` 的钉钉 userId 白名单开启，网关已传递真实客户端地址并拒绝伪造头，还需用不同客户端验证限流额度彼此独立，不打印凭据。
- 后续 PR 仍须通过全部六项门禁并取得合并批准；合并后核验新镜像 SHA、就绪、数据库与外部 HTTPS。
- 仅允许回退到已验证 Django 版本；数据库迁移不兼容时保持现场，依据对应备份人工恢复，不能自动恢复退役旧平台。

原始需求仍以 `PRD-django-single-org.md` 为准。本清单不能缩小 PRD 范围，也不能将替身测试等同于真实目录验收。
